What is DOM?
* Dom Stands For Document Object Model Which makes an Website Interactive. It defines the logical structure of documents and the way a document is accessed and manipulated.

What are the different ways to get an element from DOM?
* There are following ways to get an element from DOM- 1. getElementById 2. getElementsByClassName 3. getElementsByTagName 4. quearySelector 5. quearySelectorAll

What’s the difference between an Event Handler and an Event Listener?
* The difference is - The listener listens out for the event happening, and the handler is the code that is run in response to it happening.

What does “event bubbling” mean in JavaScript?
*Event bubbling is a method of event propagation in the HTML DOM API when an event is in an element inside another element, and both elements have registered a handle to that event.

Can you explain the different types of events available in JavaScript?
* There are many different types events available in JavaScript like- onclick(), onmouseover(), ondblclick(), focus(), blur() etc.

What’s the difference between event.preventDefault() and event.stopPropagation() ?
* preventDefault() prevents the browsers default behaviour, but does not stop the event from bubbling up the DOM. The event. stopPropagation() prevents the event from bubbling up the DOM, but does not stop the browsers default behaviour.

What is ES6? Have you ever used anything from ES6?
* ES6 Stands for EcmaScript Stands 2015. It is The Latest Version of JavaScript. Yes I have use Arrow Function, var, let, const, spread operator and such things from ES6.

Explain the difference between var, let and const.
* Var is a global scope variable. we can use same variable decleard with var anywhere in the programm. let is a block scope variable. we can not use variable with let anywhere in the programm but we can reassign the variable value with using let. and using const variable we cannot reassign or redecleard the variable.

What is the arrow function, and how to create it?
* Arrow function is one of the features introduced in ES6 JavaScript Version. It can create this way- let x = (first Parameter, second Paramenter) => what we want to return.

Give an example of an Arrow function in ES6? List down its advantages.
* let x = (first Parameter, second Paramenter) => what we want to return. The Advantages is 1. We Can omit braces when we use only one parameter 2. We can Use This function without parameter 3. It Helps to reduce code


Discuss spread operator in ES6 with an example.
* It expands the array into individual elements. So, it can be used to expand the array in a places where zero or more elements are expected. ex- const arr = [1, 2, 3, 4], const newarr = [...arr, 6] Output Will Be = [1, 2, 3, 4 , 6]

What do you understand about default parameters?
* The default parameter is a way to set default values for function parameters a value is no passed in

What are template literals in ES6?
* Template literals are a new feature introduced in ECMAScript 2015/ ES6. It provides an easy way to create multiline strings and perform string interpolation.

Tell us the difference between arrow and regular function.
* The Difference Between arrow and regular function is 1. We cann't ommit braces in regular function but can do it an arrow function 2. We can't use same parameter in arrow function but we cand do this in regular function 3. arrow function helps to reduce codes.
